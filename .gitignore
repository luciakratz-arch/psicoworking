# Segredos — NUNCA versionar
functions/.env
functions/.env.*
*-service-account*.json
*serviceAccountKey*.json
.firebaserc.local
# Senhas das extensões (ex.: senha de app do Gmail usada pelo envio
# de e-mail). O arquivo .env da extensão pode ir pro repositório, o
# .secret.local NUNCA.
extensions/*.secret.local

# Dependências
functions/node_modules/
node_modules/

# Logs e cache do Firebase
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log

# Sistema
.DS_Store
Thumbs.db
